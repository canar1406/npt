import React from 'react';
import DocSidebarContainer from '@theme-original/DocSidebarContainer';

export default function DocSidebarContainerWrapper(props) {
  return (
    <DocSidebarContainer {...props} />
  );
}
